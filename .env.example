# Example environment variables for timeless-watches
# Copy to .env and fill values (do NOT commit .env to git)

# Server
PORT=4000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:4000

# Email (optional - needed to send order notifications)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=you@example.com
EMAIL_PASS=supersecret
NOTIFY_TO=notify@example.com

# JWT / Auth
JWT_SECRET=replace_with_a_strong_random_secret

# Admin user for product management
ADMIN_USER=Admin@123
ADMIN_PASS=Abcd@1234

# Firebase Configuration (for image uploads)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Place it in project root as 'firebase-service-account.json' or set custom path below
FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Other
# Add any other secrets here
